<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 环境变量，便于区分开发和生产环境 -->
    <property name="LOG_HOME" value="./logs" />
    <springProperty name="ENV" source="spring.profiles.active" defaultValue="dev" />

    <!-- 控制台日志 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!-- 日志格式 -->
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) - [TraceID: %X{traceId}, SpanID: %X{spanId}] %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- INFO级别日志，输出到文件 -->
    <appender name="INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/info-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory> <!-- 保存最近30天的日志 -->
        </rollingPolicy>
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - [TraceID: %X{traceId}, SpanID: %X{spanId}] %msg%n
            </pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- ERROR级别日志，输出到文件 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %red(%-5level) [%thread] %logger{36} - [TraceID: %X{traceId}, SpanID: %X{spanId}] %msg%n
            </pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- DEBUG级别日志，输出到文件（仅开发环境） -->
    <springProfile name="dev">
        <appender name="DEBUG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/debug.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/debug-%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>7</maxHistory> <!-- 保存最近7天的日志 -->
            </rollingPolicy>
            <encoder>
                <pattern>
                    %d{yyyy-MM-dd HH:mm:ss.SSS} %blue(%-5level) [%thread] %logger{36} - [TraceID: %X{traceId}, SpanID: %X{spanId}] %msg%n
                </pattern>
                <charset>UTF-8</charset>
            </encoder>
            <filter class="ch.qos.logback.classic.filter.LevelFilter">
                <level>DEBUG</level>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
        </appender>
    </springProfile>

    <!-- 设置日志级别和输出 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="INFO_FILE" />
        <appender-ref ref="ERROR_FILE" />
    </root>

    <!-- 开发环境下启用DEBUG日志 -->
    <springProfile name="dev">
        <logger name="com.example" level="DEBUG" additivity="false">
            <appender-ref ref="DEBUG_FILE" />
        </logger>
    </springProfile>

    <!-- 单独设置包的日志级别（以业务包为例） -->
    <logger name="com.business" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE" />
    </logger>
</configuration>